spring:
  datasource:
    url: jdbc:postgresql://postgres.cxsztunvihuzqlwwqeuk:Sidolin%40199@aws-1-eu-west-1.pooler.supabase.com:6543/postgres
    username: postgres.cxsztunvihuzqlwwqeuk
    password: ${DATABASE_PASSWORD}
    driver-class-name: org.postgresql.Driver

    hikari:
      maximum-pool-size: 5

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID}
            client-secret: ${GOOGLE_CLIENT_SECRET}
            scope:
              - openid
              - profile
              - email

server:
  port: ${PORT:8080}

moderators:
  emails:
    - victoriaashfordthorne@gmail.com